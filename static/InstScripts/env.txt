# ==============================================================================
# CONFIGURABLE SETTINGS
# Review these values before the first container startup.
# ==============================================================================

# Public URLs used by the frontend at container startup.
# PUBLIC_MEDIA_BASE_URL should be the externally reachable media URL.
PUBLIC_MEDIA_BASE_URL=http://localhost/media/
# Public URL of the CAP service; do not include a trailing slash.
CAPTCHA_URL=http://localhost:3001

# Django host/origin security settings.
# ALLOWED_HOSTS entries are hostnames only (no scheme or path). Add your public
# domain, for example: dev.rgsscs.org
ALLOWED_HOSTS=localhost,127.0.0.1,backend,nginx
# CSRF_TRUSTED_ORIGINS entries are full origins including http:// or https://,
# for example: https://dev.rgsscs.org
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1

# Initial Django administrator account.
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@localhost

# CAP site settings (create these at http://localhost:3001 after installation).
CAP_SECRET=
CAPTCHA_VERIFY_URL=

# PostgreSQL database names.
POSTGRES_DB=db
POSTGRES_USER=db

# ==============================================================================
# NON-CONFIGURABLE / AUTO-GENERATED SETTINGS
# The installers generate secrets and keep internal service addresses here.
# ==============================================================================

# Frontend/internal application settings.
API_URL=http://backend:8000
REVALIDATE_SECRET=

# CAP dashboard and storage.
ADMIN_KEY=
REDIS_URL=redis://valkey:6379

# Django cryptographic and internal settings.
SECRET_KEY=
SIGNING_KEY=
DJANGO_SUPERUSER_PASSWORD=
REVALIDATE_URL=http://frontend:3000/console/revalidate

# PostgreSQL connection settings.
POSTGRES_PASSWORD=
DB_HOST=db
DB_PORT=5432
